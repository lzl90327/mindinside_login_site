/**
 * 产品族级品牌资源默认值（可被系统配置覆盖）。
 */
export const DEFAULT_BRAND_MARK_ICON_SRC = "/assets/brand/mindinside-logo-small.png";

export const DEFAULT_BRAND_WORDMARK = "MindInside";

/** 无系统专属 `meta.favicon` 时，标签页图标回落为品牌小标资源 */
export const DEFAULT_TAB_FAVICON_SRC = DEFAULT_BRAND_MARK_ICON_SRC;
